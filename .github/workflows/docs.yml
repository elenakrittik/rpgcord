name: Build docs

on:
  push:

jobs:
  check_formatting:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: pdm-project/setup-pdm@v3
      with:
        python-version: 3.10
        cache: true

    - run: pdm install -Gdocs
    - run: cd docs
    - run: make html
